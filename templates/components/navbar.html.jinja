<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Apollo</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <img src="/static/logo/apollo_logo.png" alt="Logo" width="45" height="45" class="rounded mx-auto d-block">


        <form class="d-flex" role="search">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="btn btn-outline-warning px-1" aria-current="page" href="/">Home</a>
                        </li>
                        <div style="padding: 5px;">
                        </div>
                        <li class="nav-item">
                            <a class="btn btn-outline-warning px-1" href="/settings">User Input</a>
                        </li>
                        {# <li class="nav-item">
                            <a class="nav-link" href="#">Pricing</a>
                        </li> #}
                        <div style="padding: 5px;">
                        </div>
                        {%if current_user.is_authenticated%}


                        <li class="nav-item my-auto">
                            <a class="btn btn-outline-warning px-1" href="/logout">Logout</a>
                        </li>

                        <div style="padding: 5px;">
                        </div>

                        <li class="my-auto px-1">
                            Welcome {{current_user.name}}
                        </li>

                        {%else%}
                        <li>
                            <a href="/sign_in" class="btn btn-outline-warning px-1">Login</a>
                        </li>
                        {%endif%}
                    </ul>
                </div>

            </ul>
            </li>
            </ul>
        </form>
    </div>
    </div>
</nav>